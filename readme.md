Petclinic based demo for Java profiling workshop
================================================

Commands
--------

 - `mvn -P bench test` - start demo (Tomcat + HSQL)
 - `mvn -P stop test clean` - stop demo